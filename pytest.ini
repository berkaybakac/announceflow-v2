[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
asyncio_default_test_loop_scope = function
testpaths = tests
markers =
    tts_smoke: real XTTS runtime smoke tests
addopts = -m "not tts_smoke"
